#include <stdio.h>
int main() {
    // int i = 0;
    int n;
    printf("Enter the value of n : ");
    scanf("%d", &n);

    // while (i <= n) {
    //     printf("%d \n", i);
    //     i++;
    // }

    for (int i = 0; i <= n; i++) {
        printf("%d \n", i);
    }
    
}